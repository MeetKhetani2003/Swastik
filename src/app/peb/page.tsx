import type { Metadata } from "next";
import { PEB } from "@/components/shared";

export const metadata: Metadata = {
  title: "PEB & Heavy Structures",
  description: "Advanced Pre-Engineered Buildings (PEB), engineered steel building systems, and heavy structural steel fabrication for industrial facilities and warehouses.",
};

export default function Page() {
  return <PEB />;
}
